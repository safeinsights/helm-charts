apiVersion: v2
name: secure-enclave
description: SafeInsights Secure Enclave — setup app, trusted output app, and research network policies for a locked-down data plane.
type: application

# Chart (package) version — bump on any template/value change
version: 1.0.1

# App version — align with your app image tag(s)
appVersion: "1.0.0"

# Prevent installs on unsupported clusters (adjust if needed)
kubeVersion: ">=1.34.0-0"

home: https://example.com/safeinsights/secure-enclave          # ← replace
icon: https://example.com/assets/safeinsights-icon.png         # ← replace

keywords:
  - safeinsights
  - security
  - networkpolicy
  - research
  - helm

sources:
  - https://example.com/docs/secure-enclave                     # ← replace
  # - https://github.com/your-org/secure-enclave               # ← replace (if public)

maintainers:
  - name: SafeInsights
    email: administrators@safeinsights.org

annotations:
  artifacthub.io/category: security
  artifacthub.io/license: Apache-2.0
  # artifacthub.io/links: |
  #   - name: Documentation
  #     url: https://example.com/docs/secure-enclave

