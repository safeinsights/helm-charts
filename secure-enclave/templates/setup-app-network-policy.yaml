apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: setup-app-network-policy
spec:
  podSelector:
    matchLabels:
      app: setup-app
  policyTypes:
    - Egress
  egress:
    - {}

